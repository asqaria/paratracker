node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example

# Реальные треки пилотов для ручной проверки — личные данные (имена в файлах и в
# заголовках IGC). В репозиторий попадают только обезличенные копии в /fixtures.
/tracks/

# Артефакты Playwright
test-results/
playwright-report/
