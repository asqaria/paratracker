packages:
  - apps/*
  - packages/*

# postinstall у этих пакетов не нужен: нативный бинарник приходит
# через optionalDependencies (unrs-resolver — eslint-import-x, esbuild — drizzle-kit и tsx).
ignoredBuiltDependencies:
  - esbuild
  - unrs-resolver
