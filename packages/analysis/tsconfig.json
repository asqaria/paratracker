{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // Типы Node — только ради тестов (чтение /fixtures). Сборка src идёт без них.
    "types": ["node"]
  },
  "include": ["src"]
}
